<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

	<mapper namespace="com.zoe.mapper.MemberMapper">

	<!-- 添加 if(判断参数) - 将实体类 User 不为空的属性作为 where 条件
	<select id="getMemberList" resultMap="resultMap_Member" parameterType="Member">
		SELECT m.member_name,
		m.member_password,
		FROM member m
		WHERE
		<if test="member_name !=null ">
			m.member_name LIKE CONCAT(CONCAT('%', #{member_name, jdbcType=VARCHAR}),'%')
		</if>
		<if test="member_password != null  ">
			AND m.member_password = #{m.member_password, jdbcType=INTEGER}
		</if>
	</select>
-->
	<select id="list" resultType="Member">
			select * from   member order by member_ID desc
	  	</select>
		<select id="list1" resultType="Member">
			select * from   member where member_name=#{memberName} and member_password=#{memberPassword}
	  	</select>
		<insert id="add" parameterType="Member" >
	        insert into member ( member_name, member_password) values (#{memberName},#{memberPassword})
	    </insert>
		<!-- 登录验证-->
		<select id="findMemberByNameAndPwd" resultType="int">
			select count(*) from member where member_name=#{memberName} and member_password=#{memberPassword}
		</select>
		<!--注册验证-->
		<select id="validateMemberName" resultType="int">
			select count(*) from member where member_name=#{memberName}
		</select>
		<!-- 登录验证-->
		<select id="findMemberByName" resultType="int">
			select count(*) from member where member_name=#{memberName}
		</select>
	</mapper>

